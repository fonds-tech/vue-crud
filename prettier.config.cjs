module.exports = {
  // 不在语句末尾添加分号
  semi: false,
  // 缩进使用2个空格
  tabWidth: 2,
  // 不使用制表符进行缩进
  useTabs: false,
  // 单行代码最大长度为180个字符
  printWidth: 180,
  // 使用双引号而不是单引号
  singleQuote: false,
  // 自动检测并使用适当的行尾字符（\n 或 \r\n）
  endOfLine: "auto",
  // 箭头函数的参数始终使用括号，即使只有一个参数
  arrowParens: "always",
  // 在对象、数组等的最后一个元素后添加逗号
  trailingComma: "all",
  // 在对象字面量的括号内侧添加空格
  bracketSpacing: true,
}
